# Pub workspace root — unifies dependency resolution across all packages.
# This is not a real package; it ties the Flutter app and pure-Dart core together
# so one `flutter pub get` resolves both with a shared lockfile.
name: _hearth_workspace
publish_to: none

environment:
  sdk: ^3.12.2

workspace:
  - app
  - core
