# macOS
.DS_Store

# Dart / Flutter
.dart_tool/
build/
.flutter-plugins
.flutter-plugins-dependencies

# IDE
.idea/
.vscode/
*.iml

# Node / TypeScript backend (future)
node_modules/
dist/
*.log

# Env / secrets — never commit these
.env
.env.*
*.pem
*.p8
serviceAccount*.json
google-services.json
GoogleService-Info.plist

# Firebase (future)
.firebase/
*-debug.log

# Claude Code per-machine local settings (not shared)
.claude/settings.local.json
