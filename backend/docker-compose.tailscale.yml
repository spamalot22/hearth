# Hearth relay — a self-contained, public Tailscale-Funnel stack. Deploy it as a
# Portainer stack (Git repo or web editor) or `docker compose -f docker-compose.tailscale.yml up -d`.
# NOTE: keep relay env/volumes in sync with docker-compose.yml.
# Anyone can run it: it pulls the published relay image and exposes it publicly via
# Tailscale Funnel — no domain, no port-forward, no Cloudflare. The relay shares the
# Tailscale container's network namespace, so Funnel reaches it on localhost and the
# relay is never otherwise exposed.
#
# Everything is configured with environment variables (Portainer → Stack → Environment):
#   TS_AUTHKEY     (required) reusable, non-ephemeral Tailscale auth key (admin console → Keys)
#   TS_HOSTNAME    node name → https://<TS_HOSTNAME>.<your-tailnet>.ts.net   (default: hearth-relay)
#   TS_USERSPACE   "false" (default) = kernel mode, REQUIRED for Funnel — userspace mode
#                  configures Funnel but never receives inbound public traffic (you get a
#                  502). Needs /dev/net/tun on the host (load the `tun` kernel module if missing).
#   GIPHY_KEY      (optional) GIF-search proxy key
#   FREESOUND_KEY  (optional) sound-search proxy key
#   TAG            (optional) relay image tag (default: latest)
services:
  tailscale-config:
    # Stage the inline Compose config into a regular directory. Tailscale watches
    # the parent directory and does not reliably support single-file mounts.
    image: tailscale/tailscale:v1.98.9@sha256:f15d5d3f4a68773a853180b72496f70ba614b64de0878c43fe3da39fe0afba47
    entrypoint: ["/bin/sh", "-ec"]
    command: ["cp -f /funnel.json /config/funnel.json && chmod 0444 /config/funnel.json"]
    configs:
      - source: ts-funnel
        target: /funnel.json
    volumes:
      - tailscale-config:/config
    restart: "no"

  tailscale:
    # Keep this deliberate: Watchtower replacing `latest` previously left the
    # node connected but without its Funnel configuration.
    image: tailscale/tailscale:v1.98.9@sha256:f15d5d3f4a68773a853180b72496f70ba614b64de0878c43fe3da39fe0afba47
    environment:
      TS_AUTHKEY: ${TS_AUTHKEY:?set TS_AUTHKEY to a Tailscale auth key}
      TS_HOSTNAME: ${TS_HOSTNAME:-hearth-relay}
      TS_USERSPACE: ${TS_USERSPACE:-false}
      TS_STATE_DIR: /var/lib/tailscale
      TS_SERVE_CONFIG: /config/funnel.json
    volumes:
      - tailscale-state:/var/lib/tailscale
      - tailscale-config:/config:ro
    depends_on:
      tailscale-config:
        condition: service_completed_successfully
    # Funnel needs kernel mode → a real TUN device. The host must provide /dev/net/tun
    # (load the `tun` kernel module on the host if missing; Proxmox LXC: lxc.mount.entry).
    devices:
      - /dev/net/tun:/dev/net/tun
    cap_add:
      - NET_ADMIN
    restart: always

  relay:
    image: ghcr.io/spamalot22/hearth-relay:${TAG:-latest}
    labels:
      - "com.centurylinklabs.watchtower.enable=true"
    environment:
      PORT: 8787
      GIPHY_KEY: ${GIPHY_KEY:-}
      FREESOUND_KEY: ${FREESOUND_KEY:-}
    network_mode: service:tailscale # share Tailscale's netns; Funnel → localhost:8787
    depends_on:
      - tailscale
    restart: always

configs:
  ts-funnel:
    content: |
      {
        "TCP": { "443": { "HTTPS": true } },
        "Web": {
          "$${TS_CERT_DOMAIN}:443": {
            "Handlers": { "/": { "Proxy": "http://127.0.0.1:8787" } }
          }
        },
        "AllowFunnel": { "$${TS_CERT_DOMAIN}:443": true }
      }

volumes:
  tailscale-config:
  tailscale-state:
